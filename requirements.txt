fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
python-multipart>=0.0.20,<1
pydub>=0.25,<1
faster-whisper>=1.1,<2
langid>=1.1,<2
pytest>=8,<9
httpx>=0.28,<1

# Optional local MeloTTS runtime dependencies are installed by Dockerfile.compose
# from https://github.com/myshell-ai/MeloTTS because the PyPI sdist is incomplete.
# Keep requirements.txt lightweight for local API tests; use the compose image when
# validating the real melo backend.
# Optional whisper.cpp binary + model remains a host/runtime fallback for STT.
